#include "main.h"
/**
 *_abs - print the absolute value of the input
 *@c: input to covert to absolute valur
 *Return: returns int
 *
 */
int _abs(int c)
{
	if (c < 0)
	{
		return (-c);
	}
	else
	{
		return (c);
	}
}
